/**
 * 設定 頁
 *
 * 開發狀態：Sprint 0 骨架
 * 規劃：settings/clinic、settings/users、settings/templates、settings/ai-prompts 的 sub-routes
 */
function SettingsPage() {
  return (
    <div className="placeholder-card">
      <h2>設定</h2>
      <p>settings/clinic、settings/users、settings/templates、settings/ai-prompts 的 sub-routes</p>
    </div>
  );
}

export default SettingsPage;
